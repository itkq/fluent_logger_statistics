require "spec_helper"

describe FluentLoggerCounter do
  it "has a version number" do
    expect(FluentLoggerCounter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
